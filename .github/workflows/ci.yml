name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: sudo apt-get update && sudo apt-get install --yes libwayland-dev libfontconfig1-dev libxkbcommon-dev
      - run: rustup toolchain install 1.89 --profile minimal --component clippy,rustfmt
      - run: rustup override set 1.89
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt --all --check
      - run: cargo clippy --workspace --all-targets -- -D warnings
      - run: cargo test --workspace

  platform:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - run: rustup toolchain install 1.89 --profile minimal
      - run: rustup override set 1.89
      - uses: Swatinem/rust-cache@v2
      - run: cargo test --workspace
